apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jsonrpc-wrapper.fullname" . }}-test-connection"
  labels:
    {{- include "jsonrpc-wrapper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jsonrpc-wrapper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
